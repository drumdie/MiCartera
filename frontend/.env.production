# Build de producción (web deploy + apk Android).
# Vite carga este archivo por encima de .env cuando corre `npm run build` (mode=production),
# así el apk/web de producción pega a Cloud Run y `npm run dev` sigue usando localhost:8000.
# La URL de Cloud Run es pública (no es secreto) → este archivo SÍ se commitea.
VITE_API_URL=https://micartera-backend-486793579128.southamerica-east1.run.app
VITE_USE_EMULATORS=false
